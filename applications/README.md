# Kitana Applications

This directory is reserved for Kitana-managed desktop entry templates and application launchers.

Runtime web app entries are generated by `kitana-webapp-install` into `~/.local/share/applications` so they can include the current install path and selected browser.
