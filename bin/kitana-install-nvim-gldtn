#!/bin/bash

set -euo pipefail

KITANA_DIR="${KITANA_DIR:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}"
repo_url="${KITANA_NVIM_REPO:-https://github.com/gldtn/nvim.git}"

exec "$KITANA_DIR/bin/kitana-install-extras-nvim" --git --repo "$repo_url"
