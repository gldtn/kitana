# Services Components

| Component | Source | Summary |
|-----------|--------|---------|
| [`AppSearchService`](AppSearchService.md) | `AppSearchService.qml` | AppSearchService is a singleton service in the Services module that exposes shared state or command helpers to the rest of the shell. |
| [`CaffeineService`](CaffeineService.md) | `CaffeineService.qml` | CaffeineService is a singleton service in the Services module that exposes shared state or command helpers to the rest of the shell. |
| [`MediaService`](MediaService.md) | `MediaService.qml` | MediaService is a singleton service in the Services module that exposes shared state or command helpers to the rest of the shell. |
| [`NotificationService`](NotificationService.md) | `NotificationService.qml` | NotificationService is a singleton service in the Services module that exposes shared state or command helpers to the rest of the shell. |
| [`OsdService`](OsdService.md) | `OsdService.qml` | OsdService is a singleton service in the Services module that exposes shared state or command helpers to the rest of the shell. |
| [`QuickshellSettings`](QuickshellSettings.md) | `QuickshellSettings.qml` | QuickshellSettings owns durable Quickshell user settings backed by `~/.local/state/kitana/quickshell-settings.json`. |
| [`SystemStatus`](SystemStatus.md) | `SystemStatus.qml` | SystemStatus is a singleton service in the Services module that exposes shared state or command helpers to the rest of the shell. |
| [`UiPreferences`](UiPreferences.md) | `UiPreferences.qml` | UiPreferences is a singleton service in the Services module that exposes shared state or command helpers to the rest of the shell. |
| [`WallpaperThumbnailCache`](WallpaperThumbnailCache.md) | `WallpaperThumbnailCache.qml` | WallpaperThumbnailCache resolves wallpaper thumbnail cache paths and helper command locations. |
